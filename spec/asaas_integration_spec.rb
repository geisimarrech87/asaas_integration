# frozen_string_literal: true

RSpec.describe AsaasIntegration do
  it "has a version number" do
    expect(AsaasIntegration::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
